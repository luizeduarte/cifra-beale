TODO: ajeitar num_chave
TODO: funcao seleciona_chaves, o que fazer quando nao encontra uma chave?
TODO: em insere_lista, verificar se eh o primeiro elemento ou menor que ele 


void adiciona_caractere(char letra, struct caracteres* aux){
	struct caracteres* novo = aloca_caractere(letra);

	if (aux == NULL){
		aux = novo;
		return;
	}
	
	novo->prox = aux->prox;
	aux->prox = novo;
}

void insere_lista(struct caracteres* lista_chars, int* num_chave, char letra){
	struct caracteres* aux = lista_chars;

	if (lista_chars == NULL){	//se a lista estiver vazia, adiciona o primeiro elemento
		adiciona_caractere(letra, lista_chars);
		adiciona_chave(num_chave, lista_chars);
		return;
	}	
